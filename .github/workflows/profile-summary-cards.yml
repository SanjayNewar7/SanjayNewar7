name: GitHub-Profile-Summary-Cards

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Generate Cards
    steps:
      - uses: actions/checkout@v3
      - uses: vn7n24fzkq/github-profile-summary-cards@release
        with:
          USERNAME: SanjayNewar7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
